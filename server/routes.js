const express = require('express');
// Add controller requirments here;

const router = express.Router();

// Map each endpoint to router method.
// router.post('/add', controller.method);

module.exports = router;